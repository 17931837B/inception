[www]
; 実行ユーザーとグループ
user = www-data
group = www-data

listen = 9000

; プロセス管理の設定
pm = dynamic
pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3

; コンテナ内で環境変数をPHPから取得できるようにする 
clear_env = no